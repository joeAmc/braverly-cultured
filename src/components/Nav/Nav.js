import React from "react";
import "./Nav.css";

const Nav = () => {
  const scrollTop = () => {
    window.scrollTo({ top: 0, left: 0, behavior: "smooth" });
  };

  return <div>Nav</div>;
};

export default Nav;
